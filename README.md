# gradops

Finite differences operations on GPU using CuPy.

## Requirements

- Python >= 3.10
- NumPy
- SciPy
- **CuPy (required, not installed automatically by this package)**  
  Install the CuPy package that matches your CUDA version (e.g., `cupy-cuda12x`, `cupy-cuda11x`, etc.).

